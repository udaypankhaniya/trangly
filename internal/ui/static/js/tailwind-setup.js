/**
 * tailwind-setup.js  Shared Tailwind CSS config for all Trangly pages.
 * Must be loaded AFTER the Tailwind CDN script tag.
 * Colors reference CSS custom properties so they adapt to data-theme.
 */
